const sharp = require('sharp');
const fetch = require('node-fetch');
const { Hand, Card } = require('./classes.js')


/**
 * @param {*} bot
 * @param {Hand} h
 */
async function showHand(bot, h) {
    
}